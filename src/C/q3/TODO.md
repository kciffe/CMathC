# 第三问代码 TODO 与实现记录

更新时间：2026-09-25

## 当前方案

- 第三问建立独立分析链，复用问题二的“神经活动经容积传导形成头皮 EEG”机制解释，不调用问题二的 Gabor/LGN/皮层仿真数值链。
- EEG 特征只使用 F3、Fz、F4。VisCue 和 TimeStamp 用于事件对齐。
- 第 9 通道 `Action`/`TgtAct` 仅提供应答方向、应答时刻和无应答标记，不进入 EEG 特征或预测变量。
- 保留原始应答码 `response_raw`，另生成统一码：负值映射为 -2，0 保持 0，正值映射为 +2；建模选择编码为左 -1、右 +1。按非零起始边沿计一次应答。
- 目标时刻暂按提示时刻 + 2.2 s 估计（约 0.2 s 提示加约 2 s 等待）。这是实验流程推定值，没有独立目标显示事件核验。
- `correct` 在目标真值未核实前留空。选择方向不等于正确/错误。

## 已实现并重跑

按顺序运行：

```powershell
python src/C/q3/01_audit_events.py
python src/C/q3/02_extract_trials.py
python src/C/q3/03_extract_features.py
python src/C/q3/04_cognitive_state.py
python src/C/q3/05_behavior_model.py
python src/C/q3/06_validate.py
```

- [x] `01_audit_events.py`：读取四份原始 MAT；审计 F3/Fz/F4、VisCue、TimeStamp 和第 9 通道；按时间戳与 VisCue 方向匹配 Q1 质量保留试次。
- [x] `02_extract_trials.py`：建立原始提示试次表；保存原始/统一应答码、应答方向、RT、应答缺失标记及 Q1 EEG 匹配状态。EEG 质量剔除与无应答分开记录。
- [x] `03_extract_features.py`：从质量保留 EEG 试次提取提示锁定、目标锁定 ERP 候选量及 θ/α/β 功率；另从连续 EEG 的一致滤波结果提取应答前窗。应答前窗为 [-1.10, -0.10] s，只作探索分析，不用于应答选择预测。
- [x] `04_cognitive_state.py`：构造 V/H/P 功能代理分数；不将其解释为定位脑区源或逐采样动态状态。
- [x] `05_behavior_model.py`：以第 9 通道选择为标签，使用提示锁定 EEG 状态代理做留一记录逻辑回归；RT 质量门槛未通过且无经核实截止时间，因此跳过 DDM。
- [x] `06_validate.py`：按整份记录留出验证 EEG 提示方向；输出特征差异、逐折指标、混淆矩阵和效果解释。
- [x] 整条流程成功重跑；结果在 `src/C/q3/output/`。

## 当前运行结果与解释边界

- 原始提示试次 400 个；第 9 通道检测到 400 个应答、0 个无应答标记；匹配到 Q1 EEG 质量保留试次 297 个。
- EEG 提示方向留一记录平衡准确率：提示锁定 0.460，目标锁定 0.508；总体接近随机水平。
- 选择方向逻辑回归留一记录平衡准确率：0.506；总体接近随机水平。该指标评价选择方向预测，不代表正确率或疾病诊断效果。
- 按提示 + 2.2 s 估计的 RT 中位数约 0.015 s，399/400 小于 0.1 s。由于目标时刻只是流程推定、且缺乏独立截止时间，当前不报告 DDM 参数。
- 现有数据不能估计漏答率（无应答标记数为 0），也不能在所有任务中核验正确性。
- 可在 `validation_interpretation.md` 查看效果和可能原因；其原因均为待核查假设，不是已证实结论。

## 后续需要补充核验

- [ ] 从任务记录/外部日志核实逐试次目标真实出现时刻，重新计算 RT；确认时间戳与第 9 通道的时钟语义。
- [ ] 获取任务真值后补充 `correct`，尤其不要用项目二的形状提示直接推断目标左右位置。
- [ ] 若有正式截止时间和质量可靠的 RT，再评估拟合 DDM；没有这些信息时保持选择模型或描述性结果。
- [ ] 核实四个文件对应的任务条件和被试/会话关系。当前留一文件验证不能称作留一被试验证。
- [ ] 依据上述核验决定是否扩展 EEG 特征或分层模型；当前样本和三条额前导联不足以支持强泛化或临床诊断结论。