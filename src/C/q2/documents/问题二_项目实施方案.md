# 问题二项目实施方案：构型驱动的低维视觉—EEG模型

版本：2026-09-24（精简首版执行版）；依据任务书及用户最新的精简决定。**本文是待实施方案，不是已通过实测验证的结果报告。** 本文取代《修正版实施计划.md》。原有01—05脚本和结果保留为基线；此前新增的`revision/`仅为未完成工程草稿。首版只完成正向模型、两个机制对照、6维实测特征的收缩LDA及同口径ERP比较；生成式判别、空间神经场、参数恢复和完整可识别性分析均为可选扩展，不阻塞首版交付。保留已经补齐的尺度、圆点控制和观测子空间规则。

## 一、第二问适配判断

### 1.1 直接结论

**当前架构可以作为第二问的机制演示起点，但还不足以完整回答第二问。** 问题不只是ERP相关系数偏低，而是“方向与位置如何组合成形状、形状差异如何保留到观测、实测是否支持这种差异”尚未闭合。

保留LGN ON/OFF、兴奋—抑制群体动力学和正向观测的思路；重排视觉层级、替换过早池化、收紧事件和观测解释。主方案选用**Gabor构型编码驱动的低维Wilson–Cowan模型**。首版只实现普通低维WC，不建立空间神经场，也不执行升级选择。

| 第二问要求 | 现有实现覆盖 | 关键缺口 | 本方案交付 |
|---|---|---|---|
| LGN→皮层→头皮EEG机制 | ON/OFF、TCR-IN-TRN、WC、线性混合均有实现 | Gabor在LGN之前；率差和物理电流混用；G无头模型依据 | 生理顺序清楚的前向链，显式源代理和观测假设 |
| 左右三角差异的形成机制 | 空间特征和模拟条件差异存在 | IT只平均方向，没有明确构型选择；形状对比读出的观测假设尚待检验 | 顶点/边缘相对位置组合、两个冻结参数机制对照及留出ERP/特征验证 |
| 可区分响应的特征表示 | 有条件平均ERP对比 | 尚非单试次留出判别；图像可分不等于EEG可分 | 固定6维ERP特征与收缩LDA的留一记录验证 |
| 与数据相符的论证 | 有四记录比较及时间尺度扫描 | 事件布局可能误标；留出相关受边界峰影响 | 保守标签、外层留一记录、边界峰与模型失败显式报告 |

波形拟合差和不能回答第二问并非同一件事：机制示意模型可以不精确复现每个峰，但必须给出可检验的机制预测。反过来，较高相关也不能证明LGN/IT机制正确，更不能补足错误的条件标签。

### 1.2 已核实的代码与数据证据

1. `03_cortex.py`的`pool_v1_to_it`把64通道压成16通道，任何同位置的方向置换都不可见；`pool_it_to_pfc`再压成左右两个空间均值，同侧内部排列也被丢掉。各WC通道独立演化，命名为IT并不自动具有配置选择性。
2. 已保存Stage1左右刺激的全局四方向Gabor相对差约`8.26×10⁻⁸`，而位置—方向张量差约`0.945`；Task2相向/背向分别约`6.75×10⁻⁸`和`0.932`。主要区分线索确实在空间排列中。方向均值投影保留的条件差平方范数约42.5%/36.1%；这只是投影几何量，不是信息保留率。
3. 原导联矩阵不仅行相似，而且**严格秩为2**：

   \[
   G_{Fz}=(G_{F3}+G_{F4})/2,
   \quad y_{F3}-2y_{Fz}+y_{F4}=0.
   \]

   奇异值约`[2.01589, 0.20100, 0]`，六源至少有四维零空间。不能靠调整时间常数解释真实EEG任意三通道拓扑。
4. 原Stage2仅有alpha瞬态驱动，未显式表示目标持续显示及适应。Stage1晚峰既受onset级联影响，也受200ms offset影响；去offset后仍有晚响应，不能把晚峰全归因于某一次事件，更不能直接称已拟合P300。
5. 原始A2/B2记录目标后约15–19ms的首事件是`±1`平台；附录所述`±2`点击在约1–2s后才出现。clean片段目标后仅覆盖约792ms，不含这些点击。即使知道点击方向，错误反应也使布局不可唯一反推。
6. 四份MAT是四条记录，无可确认被试ID。当前可确认的Stage1左右共有297试次；不能将其作为297个独立被试，也不能称四折为跨被试验证。

### 1.3 对新增候选路线的取舍

采用：带符号亮度变化、LGN后置Gabor、构型编码、低维WC、归一化固定投影、受约束观测、简单分类验证，以及去位置、去offset两个机制对照。

暂不采用：完整空间神经场、三层球头模型、自由导联估计、额外PFC/海马/LIF/Kuramoto。原因是当前缺少源坐标、头部几何、可靠被试信息和足够观测维数；增加自由度不能补足证据。可选扩展统一列在第5.6节，不进入首版执行清单。

## 二、唯一推荐架构

### 2.1 主链与输出边界

```mermaid
flowchart LR
    A[现有刺激矩阵与固定事件时序] --> B[带符号对比与中心周围滤波]
    B --> C[适应与LGN ON/OFF群体]
    C --> D[V1四方向局部Gabor响应]
    D --> E[边缘、顶点、相对位置构型]
    E --> F[固定投影后的低维WC群体]
    F --> G[突触响应代理及形状共同/对比量]
    G --> H[固定示意G的两个可观测模式]
    H --> I[模拟与实测ERP及模式差异比较]
    J[四条真实记录] --> I
    J --> K[预设6维实测特征与收缩LDA验证]
```

整个模型的坐标首先是**图像归一化坐标**，不是皮层毫米坐标。左右三角指形状方向；空间左右指图像区域；两者均不自动等于左右脑半球。只有观测向量中的F3/Fz/F4是实际电极名称。

交付三类结果并分开保存：内部构型表征、固定观测模型的EEG预测、实测EEG留出判别。任何一类成功都不能替代另外两类。

### 2.2 输入和时序

直接读取`input/*.npy`，不从示意图截图再次提取。现有矩阵作为标准化算法输入；其是否逐像素等于实验屏幕呈现未经核实，不能称已恢复物理刺激。全流程统一除以255，不再逐条件或逐条件对取最大值归一化。

对每一阶段，以相应baseline为参考：

\[
\Delta I_s(x)=\{I_s(x)-I_{0,s}(x)\}/255,
\quad C_s(x,t)=\Delta I_s(x)g_s(t).
\]

\[
g_1(t)=\mathbf1_{0\le t<200\mathrm{ms}},
\qquad g_2(t)=\mathbf1_{0\le t\le800\mathrm{ms}}.
\]

Stage1 offset通过亮度恢复自然进入动态系统，不能事后随意加一个“P300脉冲”。Stage2窗内无目标消失事件。Stage2 baseline按现有纯白矩阵，显示器gamma、亮度、视角均作为限制。

采用256×256原矩阵；前端首版面积平均至64×64。Gabor和构型在64×64上计算后才压至8×8空间网格，不能先平均到8×8再检测顶点。首版仅做下述64/128前端数值尺度检查，空间汇总网格固定8×8，不扫描4×4/16×16；尺度统一换算回原图像素。

**实现尺度约定（必须按此实现，不允许把原图像素参数原样传给降采样网格）：**256→64采用不重叠4×4像素块面积均值；图像像素中心从原网格映射到64网格的公式为`x64=(x256+0.5)/4-0.5`、`y64=(y256+0.5)/4-0.5`。以`r=256/N`表示原图与运算网格的线性比例，则所有空间长度参数在N网格上使用`pN=p256/r`，模板点坐标也按同一像素中心公式映射；时间常数不随空间分辨率换算。

| 空间算子 | 原图定义 | 64×64实际参数 | 128×128敏感性参数 |
|---|---:|---:|---:|
| DoG中心σc | 3 px | 0.75 px | 1.5 px |
| DoG周围σs | 8 px | 2 px | 4 px |
| Gabor包络σ | 4 px | 1 px | 2 px |
| Gabor波长λ | 10 px | 2.5 px | 5 px |
| 构型模糊ρ | 2 px | 0.5 px | 1 px |

64网格的Gabor波长只有2.5像素，接近离散采样的高频边界。因此正式拟合前必须运行尺度审计。对DoG核定义`E=K_DOG²`；对Gabor正交偶/奇核定义`E=K_even²+K_odd²`。令`w(x,y)=E(x,y)/ΣE`，在原图像素坐标`(r x,r y)`下计算能量质心`μ=Σw(r x,r y)`及二阶中心矩阵`M=Σw([r x,r y]−μ)([r x,r y]−μ)ᵀ`。分别在64、128和256网格上用相同定义生成核；将低分辨率核的`M`与256网格参考`M256`比较，若`||M−M256||F/||M256||F>10%`即不通过。**此处比较的是离散核能量分布的矩，不把振荡核当概率密度，也不把能量二阶矩直接等同于包络参数σ。**另记录Gabor频谱峰所对应的原图波长，偏离预设10 px超过10%即不通过。再将128网格的V1与构型图按面积均值映射回64网格；任一非零有效特征图与64网格对应图的归一化余弦相似度低于0.95也不通过，零图单独计数且不计算余弦。任一检查不通过时，不得用64网格作为主结果，改用128网格并在任何EEG参数拟合前冻结该选择。这些是预先设定的数值分辨率门槛，不是统计显著性标准；不得根据留出EEG表现选择滤波尺度。三角模板轮廓和支持点先在256网格确定，再用像素中心公式变换到运算网格；落在非整数坐标的Gabor响应统一采用双线性插值。64与128两种运算均须从同一256×256输入生成，禁止先把64结果插值成128冒充高分辨率结果。

### 2.3 视网膜/LGN：中心周围、极性和适应

以归一化高斯核构造DoG：

\[
z(x,t)=(K_{\sigma_c}-K_{\sigma_s})*C(x,t),\quad\sigma_s>\sigma_c,
\qquad \tau_a\dot a=z-a.
\]

\[
u_+=0.8[z-a]_++0.2[z]_+,
\quad u_-=0.8[a-z]_++0.2[-z]_+.
\]

初值为0，参数首版固定`σc=3、σs=8`原图像像素、`τa=80ms`。0.2持续项保证持续显示不被当成只有onset的瞬时输入；DoG的空间正负结构意味着暗刺激也可能同时激活不同位置的ON/OFF，不能把整幅暗图简单归为纯OFF。

每个极性分别保留TCR、IN、TRN：

\[
\tau_R\dot R_\pm=-R_\pm+F(2.2u_\pm-0.9N_\pm-0.8T_\pm),
\]
\[
\tau_N\dot N_\pm=-N_\pm+F(1.2u_\pm+0.7R_\pm),\quad
\tau_T\dot T_\pm=-T_\pm+F(0.9R_\pm).
\]

沿用原LGN的`τR=18、τN=12、τT=25ms`作为现象学默认值；不把它们写成已测得的人类生理参数。`F`沿用零输入输出为0的截断sigmoid，并将斜率、阈值放入唯一配置文件。分别保存ON/OFF，不只保存和；V1输入取`R=R+−R−`，另存总活动用于诊断。

明确实现为`Fβ,h(v)=clip((sigmoid(β(v−h))−sigmoid(−βh))/(1−sigmoid(−βh)),0,1)`；LGN使用`β=4,h=0.5`，皮层使用`β=5,h=0.35`，均固定。积分首版用1ms显式Euler，记录未经裁剪的状态；出现超出[0,1]超过数值容差的状态应报错，不能用逐步裁剪掩盖不稳定。

### 2.4 V1：完整保留方向与位置

\[
V_\theta(x,t)=\sqrt{(g_{\theta,e}*R)^2+(g_{\theta,o}*R)^2},
\quad\theta\in\{0°,45°,90°,135°\}.
\]

Gabor参数沿用原图尺度`σ=4、λ=10、γ=0.5`，偶/奇正交相位；核去直流并用同一种固定范数归一化。每个条件使用同一核和幅值标度。保存`V[orientation,y,x,time]`，四方向轴不在进入形状层前平均。

这里Gabor是V1风格的计算近似，不是真实皮层神经元的完整模型。中心周围→皮层方向整合的层级有实验依据，但这些具体通道数和参数是工程假设。[Hubel与Wiesel，1962](https://physoc.onlinelibrary.wiley.com/doi/10.1113/jphysiol.1962.sp006837)

### 2.5 Gabor构型编码：明确包含顶点及相对位置

首版只建**两套镜像配对的三角构型模板**`S_L、S_R`，不训练大字典，不为每条EEG记录定制模板。它们来自已知刺激的几何，不来自留出EEG或标签统计。

确定性构建步骤：

1. 从Stage1刺激相对baseline的变化像素得到三角掩膜，移除未变化的圆环；以掩膜质心为构型中心。
2. 对边界取凸包，从凸包顶点中选择面积最大的三点作为三角顶点；记录该三角与掩膜的交并比。若低于0.9，停止该模板的自动构建并输出几何检查错误，不能静默换成任意模板。当前标准矩阵应由单元测试确认满足该条件。
3. 每条边在25%、50%、75%处取样，形成9个轮廓支持点；每个顶点沿相邻两条边各向内取一个支持点，形成6个角点支持点。偏移全部相对质心保存。
4. 每个支持点使用边法线附近的Gabor方向。相邻方向按角距离线性插值，而不是为获得更好EEG结果手选方向。角点响应由其两条边支持点的几何均值形成；整体轮廓再组合三个角点与9个边支持点。
5. 左模板由左刺激确定，右模板由水平镜像确定；核方向同步作`45°↔135°`置换。独立右刺激只用于检查镜像一致性，不重新调参。

模糊—位移响应与构型响应为：

\[
\widetilde V_j(x,t)=(K_{\rho}*V_{\theta_j})(x+\delta_j,t),
\]
\[
H_k(x,t)=\left[\exp\!\left(\frac{\sum_jw_j\log(\varepsilon+\widetilde V_j)}{\sum_jw_j}\right)-\varepsilon\right]_+,
\quad k\in\{L,R\}.
\]

首版`ρ=2`原图像像素、`ε=10⁻⁸`、同层支持点等权；边界外填0，不周期回绕。三级组合分别归一化权重，避免模板支持点更多就自动获得更大幅值。模板偏移、方向和权重写入JSON并固定hash。

三角模板对所有刺激都按同一公式计算，不按Task名称、真实cue标签或答案条件手动开关。双圆点刺激作为形状特异性控制：保存其`H_L/H_R`能量及相对Stage1三角刺激的能量比；若圆点出现明显三角模板响应，应如实报告为模板交叉激活，不以真实标签调低响应或重画模板。主参数拟合和主分类只使用Stage1左/右三角提示；Task1双圆点使用冻结参数作外部控制性前向检查，不参与`τS`、`gI`、`τa`或观测幅值`a`的拟合。若需要解释双圆点本身，另行预注册圆形构型模块，不把三角模板泛化为圆点识别器。

这称为**简化Gabor构型编码**，借鉴COSFIRE的模糊、位移和几何均值，不声称完整复现COSFIRE或真实IT神经元。[Azzopardi与Petkov，2013](https://pubmed.ncbi.nlm.nih.gov/22585100/)

同时保存通用早期边缘图`B=(Σθ Vθ²)^(1/2)/2`，使dots仍有视觉驱动；dots不建立任意“左/右三角标签”。`H_L/H_R`的差异是模型图像层预测，不能单独当作EEG分类证据。

### 2.6 普通版：六个共享参数WC群体

采用图像左右半域的固定基函数`b1=√2·1[x<0]、b2=√2·1[x≥0]`，定义面积平均内积`〈f,g〉=N⁻¹Σxf(x)g(x)`，故`〈bi,bj〉=δij`。坐标和网格事先固定，所有条件共同使用；不按各刺激质心移动投影区，也不按数据重新归一化。

对三个输入场`B、H_L、H_R`分别投影：

\[
P_{k,m}(t)=\langle X_k(\cdot,t),b_m\rangle,
\quad k\in\{B,L,R\},\ m\in\{1,2\}.
\]

共六个E/I群体。先完成构型运算再投影，不能把方向平均后的图拿去假装构型输入。

\[
\tau_{E,k}\dot E_{k,m}=-E_{k,m}+(1-E_{k,m})F(w_{EE}E_{k,m}-g_Iw_{EI}I_{k,m}+g_PP_{k,m}(t-d_k)),
\]
\[
\tau_{I,k}\dot I_{k,m}=-I_{k,m}+(1-I_{k,m})F(w_{IE}E_{k,m}-w_{II}I_{k,m}+g_QP_{k,m}(t-d_k)).
\]

首版固定`wEE=1.4、wEI=1.1、wIE=1.0、wII=0.8、gP=2.2、gQ=1.2`；早期`τE=18、τI=10、dB=8ms`；形状层`τE=τS、τI=0.55τS、dL=dR=33ms`。形状层的总延迟包括8ms早期与25ms整合近似，不再与时间常数绑定缩放。左右模板、两个空间基函数和全部刺激共享这些参数。

上述六群体是低维功能模型，不宣称对应六个可定位脑源。第二问不加入PFC认知层；后续第三问如需认知状态，必须另立数据和假设。

### 2.7 源代理与观测：既不丢构型，也不伪造解剖学

率变量不直接标作电流。对E/I作单位面积突触响应卷积：

\[
h_\tau(t)=\mathbf1_{t\ge0}\,t\exp(-t/\tau)/\tau^2,
\quad p_{k,m}=h_{10\mathrm{ms}}*E_{k,m}-h_{20\mathrm{ms}}*I_{k,m}.
\]

它是**相对单位的等效源活动代理**，没有树突方向、膜面积和电导标定，不能称物理电流或μV。神经群的突触后响应代理可用于观测近似，但并不因此完成真实头皮正向模型。[David与Friston，2003](https://www.fil.ion.ucl.ac.uk/spm/doc/papers/od_neural_mass.pdf)

不能把两套形状响应又全部平均。固定保留共同与对比量：

\[
p_{\Sigma,m}=(p_{R,m}+p_{L,m})/\sqrt2,
\qquad p_{\Delta,m}=(p_{R,m}-p_{L,m})/\sqrt2.
\]

这是正交坐标变换，不增加整体范数。六维中间代理固定排列为
`s=[pB1,pB2,pΣ1,pΣ2,pΔ1,pΔ2]`。`pΔ`正负是功能对比，不是把右刺激直接赋一个电极符号；必须由两套模板和相同动力学生成，禁止以真实cue标签给源加正负号。

继承原数值矩阵，所有拟合中冻结：

\[
G=\begin{bmatrix}
.32&.24&.48&.36&.72&.58\\
.28&.28&.42&.42&.65&.65\\
.24&.32&.36&.48&.58&.72
\end{bmatrix},\quad \hat y(t)=aG s(t),\ a\ge0.
\]

**需要明确承认的假设**：旧G本来就不是实测导联场；这里将其保留为固定观测基准，并重新定义中间代理的功能含义，不能继续沿用原“V1/IT/PFC解剖贡献”的解释。形状对比源向头皮电位的固定投影是待检验的观测假设，不是由Gabor自动推导出的生理事实。首版用留出ERP、左右效应比较和去位置对照检查它；失败时不改变G补救。单独的对比源消融为可选扩展，LDA成功不能替代观测假设的验证。

实际用于分析的源模式仅为两个：令`G=U2Σ2V2ᵀ`，保存`z=Σ2V2ᵀs`和`ŷ=aU2z`。六维`s`是固定前向计算的中间量，不从EEG估计；G的四维零空间不作解释。固定SVD的符号约定，避免不同运行模式翻转。

实测观测空间另用三个正交基：

\[
u_0=(1,1,1)/\sqrt3,\quad u_1=(-1,0,1)/\sqrt2,\quad u_2=(1,-2,1)/\sqrt6.
\]

模型只能解释`u0/u1`中的预测，`u2ᵀŷ=0`；真实`u2ᵀy`的能量和条件差异必须报告，不能丢弃后宣称全拓扑拟合良好。不写`y_R=flip(y_L)`，而是两个刺激独立前向计算；刺激镜像只约束编码，不作为实测F3/F4镜像约束。功能对比源允许模型共同模式也随形状改变，但其真实性必须由数据检验。

### 2.8 首版范围封口

只执行第2.2—2.7节的普通低维模型。没有空间神经场、嵌套模型升级、生成式似然或参数恢复依赖。不要把固定投影后的WC称为严格Galerkin空间场求解。可选研究只保留在第5.6节，首版交付前不开展。

## 三、事件和条件编码的保守规则

### 3.1 唯一事件表

| 阶段 | 模型参考点 | 实测baseline | 主比较窗 | 晚期诊断窗 | 额外要求 |
|---|---|---|---|---|---|
| Stage1 | cue onset=0ms，offset=200ms | [-0.2,0)s | [0,800]ms | [450,700]ms | 同时报offset后[250,500]ms；onset后[250,500]只作次要混合事件诊断 |
| Stage2 | target onset=2.20s | [2.0,2.2)s | [0,800]ms | [250,500]ms | 主窗只取真实存在的采样点，约到792.19ms；不外推实测尾端 |

保留Stage2 [0,500]ms作为与旧05结果的附加对照，但不能用它与新全窗结果混算提升。模拟以1ms步长产生完整0–800ms，在真实128Hz时间点插值采样；不把真实EEG插值上采样后当成更多独立样本。

当前统一使用固定时序假设。有明确语义且独立记录的刺激触发后，才可按每试次触发对齐，并重新生成全部比较；`Action/TgtAct`首沿不满足该条件。缺baseline、标签重复、时间轴不一致、非有限EEG直接报错，不悄悄补0。

### 3.2 条件规则和有效样本

| 记录 | Stage1左/右 | Stage2最终标签 | 主拟合/控制用途 |
|---|---:|---|---|
| VisualCogA_Task-1 | 29/25 | dots，54 | Stage1左右主拟合；dots冻结参数控制 |
| VisualCogA_Task-2 | 45/35 | dots，80 | Stage1左右主拟合；dots冻结参数控制 |
| VisualCogB_Task-1 | 41/42 | target_unknown，83 | 仅Stage1 |
| VisualCogB_Task-2 | 37/43 | target_unknown，80 | 仅Stage1 |

拟合清单固定为8个Stage1左右主条件（4份MAT×2方向）；另有2个VisualCogA Stage2双圆点控制条件和2个VisualCogB Stage2未知布局汇总条件。后4项均不进入主模型参数、幅值系数或主分类器拟合；双圆点仅用于冻结参数的外部控制检查，未知布局仅作总体ERP描述。每个主条件仍须满足`n≥10`门槛。

Task1/Task2指实验项目A/B，不要把MAT文件名后缀`Task-1/Task-2`误当不同项目。原B1的2/81与B2的40/40布局计数保留在历史审计中，**不作为可靠标签继续使用**。

- Stage1只认`cue_type=-1/+1`；其余值隔离并计数。
- Task2 Stage2模拟仍可生成inward/outward作机制反事实，但不能与伪标签真实ERP配对；真实只保存unknown总体ERP。
- 不用50/50假定比例混合两个模拟去拟合unknown，也不估计未知比例来偷偷恢复标签。
- 每条记录每条件`n<10`仅描述，不进入参数拟合、条件效应推断或分类评价。
- 单试次先按预设baseline减均值再计算ERP/特征；这个固定的试次内操作不使用测试标签。其他数据驱动标准化、平滑、降维均训练折内拟合。
- 原题的点击可能错误或缺失；即使日后读到±2，也须有独立正确性和目标布局日志才可恢复布局。

真实单位沿MAT元数据核实；无法核实时标“数据原始单位”，模拟始终为相对单位。不得将无校准的真实/模拟幅值都写成μV。

## 四、逐文件实施与执行顺序

### 4.1 保留基线，建立精简独立修正版

原01—05及两个诊断脚本均保留，不通过修改全局变量或覆盖输出制造“新旧对比”。新入口放在`q2/revision/`，正式运行输出放在`q2/output/revision_v1/`；此前`output/revision/`是工程草稿输出，不混入验收。

| 修正版文件 | 职责 | 合并的旧流程 |
|---|---|---|
| `config.py` | 路径、时间窗、固定参数、刺激/模板定义及观测矩阵 | 统一各脚本中的常量，不保存运行结果 |
| `real_data.py` | 读取MAT、事件/条件审计、ERP与试次数组 | `05_simulated_real_eeg_compare.py`及旧诊断的数据入口 |
| `frontend.py` | 固定刺激矩阵、LGN ON/OFF、Gabor与构型编码 | `01_gabor_frontend.py`、`02_lgn.py`及新构型步骤 |
| `model.py` | 六群体低维WC、源代理、固定示意观测 | `03_cortex.py`、`04_simulated_eeg.py`的修正版前向链 |
| `fit.py` | 三个共享参数的有界拟合、逐折幅值估计与留一MAT切分 | `05_2_fullcurve_calibration.py`的有限拟合部分 |
| `evaluate.py` | ERP指标、6维收缩LDA、数值/映射诊断、两项机制对照及作图 | `05`、`03_2`、`04_2`中首版实际需要的分析 |
| `run_revision.py` | 顺序调用审计、前向模拟、拟合、评价并写出汇总 | 新版唯一运行入口 |
| 运行与验收报告 | 历史结果不覆盖；记录通过、失败与不可评估项目 | 各阶段说明文档中的新版结果摘要 |

现有`revision/model.py`按上述7个模块整理；不要再为每个分析步骤新建一个小脚本。不能因为草稿已能产生曲线就视为最终实现。尤其当前草稿的配置等权读出会抹掉某些方向身份，必须由第2.7节的显式对比读出及其反例测试替换。

### 4.2 接口与数据契约

统一轴约定为`time`最后一维；所有结果携带真实`time_ms`，禁止以数组长度重新猜时间轴。`config.py`集中保存路径、时序、默认参数、随机种子`20260924`、规范刺激/模板定义和冻结观测矩阵；运行结果只由`run_revision.py`写入输出目录。

```python
# frontend.py
load_stimulus(stage, condition) -> Stimulus
# fields: image, baseline, signed_contrast, provenance, pixel_scale
simulate_frontend(stimulus, events, params, time_ms) -> FrontendResult
# LGN ON/OFF、4方向V1响应及构型图；数组时间轴统一放在最后一维

# model.py
simulate_forward(frontend, params, fixed_observation, time_ms) -> ModelResult
# WC E/I、source_proxy [6,time]、observable_modes [2,time]、eeg [3,time]
# 不读取真实条件标签；G与基函数从config.py读取且冻结

# real_data.py
load_cases_with_audit() -> (list[Case], list[AuditRow])
# Case: record/task/stage/condition/n_trials/time_ms/trials/erp/eligible/reason
# trials: [trial, 3, time]，电极顺序固定F3/Fz/F4

# fit.py / evaluate.py
fit_model(train_cases, model_spec, seed) -> FitResult
extract_features(trials, time_ms, feature_spec) -> array  # [trial,6]，仅u0/u1
fit_classifier(train_features, train_labels, train_record_ids) -> Classifier
# 保存训练标准化、类别均值、共享收缩协方差；不接收模拟模板
predict_classifier(frozen_classifier, heldout_features) -> (scores, labels)
evaluate_fold(frozen_fit, frozen_classifier, heldout_cases, baseline) -> FoldResult
```

`FitResult`保存训练记录名、参数、幅值系数、边界标记、每个初值损失、收敛状态及输入hash。`FoldResult`保存测试记录名、逐条件指标、试次预测和排除原因。评估接口不接收优化器，也不允许改写`FitResult`。

### 4.3 首版执行顺序与停止点

| 顺序 | 完成内容 | 必须留下的证据 |
|---|---|---|
| 1 | 统一配置和数据清单 | 8个Stage1主拟合条件＋2个圆点控制＋2个unknown汇总；电极排序、baseline和未知码隔离 |
| 2 | LGN→V1→构型 | 尺度换算和数值审计、零输入、镜像编码、模板几何、圆点交叉响应 |
| 3 | 普通WC→源代理→固定观测 | 状态有界、基函数归一化、G秩2、差异沿链是否保留；dt减半检查 |
| 4 | 默认主链及有限拟合 | 四折外层留一MAT：每折只用其余3份记录的Stage1左右条件拟合；在留出记录评估Stage1。该折参数冻结后可检查双圆点；inward/outward只作规范刺激驱动的模拟反事实，不与B记录unknown ERP配对。保留失败折 |
| 5 | ERP比较和两个机制对照 | 旧1.50、新默认、新拟合；去位置和去offset冻结参数，不额外优化 |
| 6 | 6维实测特征＋收缩LDA | 逐记录试次分数、类别、BA、AUC及混淆矩阵；训练测试隔离 |
| 7 | 报告并结束首版 | 机制链、ERP及左右效应、分类、两项对照、限制与失败说明 |

第7步完成即交付，不因可选扩展未实现而继续拖延。先做一折烟雾测试再跑四折，不做架构搜索。修改留出数据或标签不能改变训练参数；只改变u2而保持u0/u1不变时，主分类分数应不变。前端缓存按输入、分辨率及影响它的参数（包括τa）区分，不能跨参数误复用。

以下是待实现入口，不表示当前草稿已支持：

```powershell
./.venv/Scripts/python.exe -m pytest src/C/q2/revision/tests -q
./.venv/Scripts/python.exe src/C/q2/revision/run_revision.py --mode audit
./.venv/Scripts/python.exe src/C/q2/revision/run_revision.py --mode simulate
./.venv/Scripts/python.exe src/C/q2/revision/run_revision.py --mode validate --model ordinary
```

正式输出收敛为6类数据文件：`manifest.json`（配置、刺激/模板定义、hash、折信息和运行状态）、`condition_audit.csv`（条件计数、事件审计与排除原因合并）、`fold_summary.csv`（每折参数/边界状态、ERP与LDA指标）、`trial_scores.csv`（留出试次的真实标签、判别分数和预测标签）、`mechanism_controls.csv`（两项机制对照）、`model_outputs.npz`（必要的时间轴与作图曲线；不重复保存可重算的中间数组）。仅生成3张综合图：机制链、留出ERP比较、分类与机制对照。旧基线运行前后校验hash；历史扁平聚合目录不参与主比较。

## 五、首版拟合、分类验证与完成标准

### 5.1 少量参数与有限估计

先运行固定默认参数，再仅拟合三个共享参数；其他核、模板、延迟、基函数和G固定，不解释这些参数为真实生理常数。

| 参数 | 默认 | 界限 |
|---|---:|---:|
| 形状时间常数τS | 40ms | [20,100]ms |
| 抑制倍率gI | 1.0 | [0.5,1.5] |
| 适应时间τa | 80ms | [40,160]ms |

左右共享参数，不加入记录专属增益、偏移或时移。唯一非负观测系数a用训练记录解析估计，对所有电极和条件相同，测试时冻结。采用默认值加2个固定种子初值，有界优化每初值最多60次目标评估；不扩大界限或追加大扫描。记录收敛、损失、是否落在界限前后2%范围及各初值结果。非有限数或状态越界候选无效。

参数不稳定或卡边界就报告，保留默认对照；首版不做完整可识别性分析或合成恢复，也不声称估计值具有生理真实性。

### 5.2 留一记录拟合与公平ERP比较

四次完整留出一份MAT，其余三份训练。这是跨记录迁移检查，不是跨被试验证。每折仅用训练记录的Stage1左右条件估计共享动力学参数和非负幅值系数；留出MAT的Stage1左右ERP与试次用于评价，不能进入拟合、标准化或分类器训练。该折拟合参数冻结后，双圆点仅作前向控制；B记录Stage2 `target_unknown`只报告总体ERP。`inward/outward`只由规范化刺激矩阵生成模型反事实，不作为B记录真实布局标签或ERP配对条件。训练记录内左右等权，再记录等权。

训练案例i在实测采样点的无尺度预测为m_i(θ)、真实ERP为e_i，〈A,B〉_i为该案例通道与时间元素的平均内积，w_i为上述权重：

```text
a(θ) = max(0, Σ_i w_i〈m_i,e_i〉_i / Σ_i w_i〈m_i,m_i〉_i)
J(θ) = Σ_i w_i〈e_i − a(θ)m_i, e_i − a(θ)m_i〉_i
```

分母为0的候选无效。选择训练损失最低的有效候选，不按留出峰时间或分类结果重新选择。全部候选无效时采用数值有效的默认模型并标记拟合失败；默认模型也无效则停止该折报错，不伪造预测。

晚峰位于诊断窗首/尾两个实际采样点时标为截断；无内部正局部峰时标为无正峰。首版不以“必须拟合出晚峰”为约束筛选参数，不靠拉长时间常数造峰。截断或无峰的案例不能宣称已解释相应晚峰。

旧1.50、新默认、新拟合用相同8个Stage1条件、采样窗和记录权重；旧基线也仅在训练折估计一个全局幅值系数。原任务书r=0.322等采用不同条件口径，不能直接比较；圆点单列，不并入主平均。

首版不做特征搜索、数据驱动平滑、空间模型选择或嵌套交叉验证；尺度按第2.2节数值审计预先冻结。若q1去噪曾用全数据学习统计量，应训练折重估；暂不能重做时明确评价以既有离线清理数据为条件，不声称前瞻泛化。

### 5.3 6维实测特征与一个收缩LDA

只对Stage1做左右分类。每试次在三个预设窗`[80,200)、[250,450)、[450,700]ms`计算共同模式u0和侧化模式u1的均值，得到6维f01。u2仅作未解释拓扑诊断，不进入主分类。窗、模式和维数不根据测试结果更换。

分类器完全使用真实训练试次，**不由模拟模板构造似然、不实现生成式Λ、不拟合共享模板残差校正b**。模拟与真实的联系通过相同模式和时间窗的左右效应比较建立；LDA效果只验证实测特征是否可分。

固定baseline减均值后，仅用训练试次估计标准化均值和标准差（零标准差按1）。训练记录总权重相同，每记录内两类权重相同，每类试次等权；标准化、类别均值和类内残差协方差均按这些训练权重估计。

记标准化特征x、真实训练类别均值μL/μR、共享类内协方差S，固定λ=0.1：

```text
Σ = (1−λ)S + λ·tr(S)I/6 + 10⁻⁶I
w = Σ⁻¹(μR−μL)
d(x) = wᵀ[x − (μR+μL)/2]
```

等先验下d>0判右，否则判左；保留平局数。这是LDA分数，不是视觉前向模型的生成证据。收缩只作为分类器的数值稳定措施，不独立研究或调参。保存测试分数后再用测试标签计算指标，禁止在测试集翻转方向或选择阈值。

报告每记录平衡准确率BA、ROC-AUC、两类召回率、混淆矩阵及四记录等权均值。BA=0.5仅为机会水平参考，不是统计显著性阈值。首版不再叠加其他分类器或9维LDA对照。

### 5.4 两个机制对照与基本数值检查

对照采用完整模型的冻结参数、同一G和幅值系数，不重新拟合，不训练额外分类器。

| 对照 | 操作 | 比较与解释 |
|---|---|---|
| 去位置 | 每方向V在空间平均后广播，保持该方向总量，再执行同一构型和动力学 | 比较左右构型差、源差、EEG差及整体幅值；不能把一般响应减弱全归因于形状选择性 |
| 去offset | Stage1持续到800ms，不在200ms恢复baseline，其余不变 | 比较晚峰形状、潜伏期和源活动；只说明模型对offset的依赖，不称真实P300成分 |

它们回答“模型是否利用排列”和“晚响应是否受offset影响”，不证明真实脑的因果机制，也不单独证明构型提高了留出分类率。其他无构型、无反馈、无对比源、重拟合消融与大规模扰动都不是首版任务。

保留正确性检查：尺度/坐标一致、零输入、镜像编码、持续目标、圆点交叉响应、构型→源→观测的差异追踪、基函数归一化、G秩与第三模式、训练测试隔离。对Stage1左右默认响应比较dt=1和0.5ms；相对L2误差>5%时减小步长或修复数值实现，不能调模型参数掩盖。全零曲线单列，不把其相对误差为零当有效机制。

### 5.5 完成标准与结果解释

**区分首版工作完成和模型获得实测支持。** 完成不以人为指定的相关增益或准确率为前提；模型表现差仍须完整交付并如实降级。

首版完成须具备：

1. 从输入到三电极的可重复主链、明确方程与参数，正确性检查通过，无未说明数值失败。
2. 左右差异沿构型、群体、源代理、观测逐层展示，两个冻结参数机制对照完成。
3. 四次留一记录ERP和6维LDA评价完成；所有失败、排除、无峰和截断案例保留，不删除表现差的记录。
4. 报告真实与模拟的右减左效应、共同/侧化模式、未解释u2能量及旧基线同口径比较。
5. 保存输入/代码/参数hash、主要表图、结论和限制；不要求实现第5.6节扩展。

| 指标 | 统一口径 |
|---|---|
| 波形r | 每通道带符号相关，无时移；左右等权再记录等权；零方差记不可评估 |
| 幅值 | RMSE及以训练真实ERP加权RMS为分母的NRMSE；不在测试集重新缩放 |
| 拓扑与条件效应 | 预设窗三电极均值、共同/侧化右减左、u2能量比例；近零拓扑余弦记不可评估 |
| 晚峰 | onset/offset双参考；双方均有未截断正峰才算误差，同时报告有效数 |
| 分类 | 逐记录BA、AUC、召回率、混淆矩阵，不将ERP平均当单试次 |
| 机制对照 | 冻结参数前后差异及总幅值，不与重拟合消融混算 |

取消旧版“平均r至少+0.02”“BA至少0.55且不得落后LDA0.02”“必须3/4记录改善”及空间版升级门。仍完整报告效应大小和逐记录一致性，不能为了通过门槛继续盲调，也不能把完成实现描述成预测成功。

| 结果情况 | 结论边界 |
|---|---|
| 实现错误或数值失败 | 实现未完成，先修复，不写机制主结论 |
| 内部差异明确，ERP匹配弱 | 机制示意与可检验假设，不能说解释了实测全部差异 |
| LDA可分，前向条件效应不吻合 | 实测特征含判别信息，机制/观测链仍不足；分类不为机制背书 |
| LDA接近机会水平或跨记录不稳定 | 当前特征缺少稳定区分证据，不能声称可靠分类已解决 |
| 模拟与实测效应一致且分类稳定 | 对计算假设提供有限支持，不证明唯一机制、解剖定位或跨被试泛化 |
| 参数卡边界或初值结果不同 | 报告估计不稳定，保留默认对照，不解释精确生理值，不追加无界搜索 |

### 5.6 可选扩展：不在首版执行清单内

首版交付后，围绕具体问题另行决定是否开展：

- **生成式似然比**：检验模拟模板能否直接分类时，可在u0/u1六维空间建立工作似然；u2不能经协方差间接参与。这不是完成第二问的前置条件。
- **完整可识别性和参数恢复**：需要论证内部参数时才做系统敏感性、合成恢复等研究；首版不作生理参数真值声明。
- **空间神经场**：低维模型暴露明确空间不足且有数据支持时再立项，届时单独设计训练内比较，不保留现成升级门催促实现。
- **扩展消融/扰动、9维经验分类、圆形构型模块**：解决首版留下的问题，不一次性全部加入。

## 六、论文结论边界与交付状态

### 6.1 可以直接使用的模型说明

> 本文建立带符号视觉对比、LGN ON/OFF、局部方向及形状构型的现象学前向模型，通过低维兴奋—抑制群体与固定示意混合产生头皮观测代理。模型用于提出和检验视觉差异的计算假设，不用于六源反演或解剖定位。

> Stage1包含提示出现与200ms后的消失，分别报告两种事件参考潜伏期。去offset对照分析模型晚响应对事件时序的依赖，不将模拟峰直接鉴定为P300；模拟幅值为相对单位。

> 采用预设时间窗内共同和侧化模式的6维实测特征，通过收缩LDA进行留一MAT记录评价。分类检查实测特征的区分能力，与前向ERP解释分别报告。被试身份未知，结果不构成跨被试泛化证据。

### 6.2 必须由实际结果限定的表述

- 图像构型可分不能代替EEG可分；LDA结果须来自留出单试次。
- LDA效果好不能证明LGN/皮层参数或G真实，须分别检查模拟与实测条件效应。
- 冻结参数去位置只支持“模型利用排列”；若声称构型提高留出预测，须另做公平重拟合比较，首版不作该声明。
- 晚峰解释须报告offset对照、无峰与截断，不能仅凭潜伏期命名P300。
- 当前不能声称特定半球/脑区来源或生理参数真值；缺少头模型和独立参数证据。
- 圆点只作冻结参数控制；Task2未知布局只作ERP汇总，不能推断标签补齐结果。
- 模型匹配弱或分类不稳定要如实报告，不把实现完成写成模型已验证。

### 6.3 文档与实现状态

本版已同步精简范围、7个代码模块接口、逐折留出规则、有限拟合、6维LDA、两个机制对照、精简输出和完成标准。此前revision仍是未完成草稿，output/revision不是正式验证结果。

本次只修改文档，不改动或运行模型代码。后续依第4.3节实施普通低维主链，完成第7步即交付；可选扩展不阻塞首版。原基线和用户任务书不覆盖。
