# 问题二代码实现 TODO

> **For agentic workers:** 按任务顺序逐项实施；执行代码计划时使用 `superpowers:executing-plans`。每项通过对应验收条件后再勾选。

**目标：** 在保留原始 `01—05` 脚本和结果的前提下，完成一个可复现的 LGN→视觉构型→低维 Wilson–Cowan→三电极观测正向模型，并用同一批真实记录完成留出 ERP 比较和左右响应分类。

**架构：** 在 `q2/revision/` 整理为 7 个运行模块：`config.py`、`real_data.py`、`frontend.py`、`model.py`、`fit.py`、`evaluate.py`、`run_revision.py`。旧版诊断代码中首版需要的检查合并进 `evaluate.py`；不再按每个小步骤新增脚本。

**技术环境：** 使用项目现有 Python 环境及已安装依赖；实现前确认 MAT 读取、数值计算和绘图所需库，不为可选扩展添加新依赖。

**依据：** [《问题二项目实施方案》](./问题二_项目实施方案.md)

**当前状态（2026-09-24）：** 七个运行模块已完成首轮方案化实现并落入 `revision/`；尚未执行运行、尺度审计或验收，因此下列检查框保持未勾选。实施状态见 [revision README](../revision/README.md)。

## 全局约束

- 原 `01_gabor_frontend.py`、`02_lgn.py`、`03_cortex.py`、`04_simulated_eeg.py`、`05_simulated_real_eeg_compare.py` 及历史诊断脚本保留，不覆盖、不改写结果。
- 新版代码放在 `src/C/q2/revision/`，正式输出放在 `src/C/q2/output/revision_v1/`；不与 `output/revision/` 草稿混用。
- 主拟合只用 4 份 MAT 的 8 个 Stage1 左右条件。两个 A 记录 Stage2 双圆点只作冻结参数控制；两个 B 记录 Stage2 只作 `target_unknown` 总体 ERP 描述。
- `inward/outward` 仅为标准刺激矩阵生成的模型反事实，不与未知布局的真实 ERP 配对。
- 首版不做生成式似然、空间神经场、参数恢复、完整可识别性、大规模参数扫描或为通过准确率门槛而调参。

## 实施审查重点

- **未知标签与条件分组：** `real_data.py`必须逐MAT保留记录边界；B记录Stage2保持`target_unknown`，不得按答案或猜测补标签。
- **离散空间尺度：** `frontend.py`从同一256×256刺激分别生成64/128网格结果；尺度审计不通过时，先冻结采用128网格，再进行EEG拟合。
- **onset/offset极性：** Stage1的0 ms出现与200 ms消失分别输入；Stage2目标在模拟窗内持续显示，不制造offset。
- **训练/留出隔离：** `fit.py`和`evaluate.py`每折独立训练；标准化、参数、幅值系数和LDA均不得读取留出记录。
- **观测秩限制：** `model.py`固定G秩为2；`evaluate.py`报告`u2`未解释成分，不把模拟与实测的拓扑缺口藏起来。

## 文件清单

| 文件 | 当前状态 | TODO 职责 |
|---|---|---|
| `revision/config.py` | 已按方案整理，待运行确认 | 汇总固定路径、时间、参数、模板几何及冻结观测矩阵，避免重复常量 |
| `revision/real_data.py` | 已按方案调整，待数据审计确认 | 读取 MAT、整理条件、执行事件与质量审计、形成 ERP/试次数组 |
| `revision/frontend.py` | 已实现主链代码，尺度门待执行 | 固定视觉刺激、LGN ON/OFF、Gabor 与三角构型编码 |
| `revision/model.py` | 已重写主链，待数值运行 | 六个 E/I 群体、突触响应源代理、固定示意观测 |
| `revision/fit.py` | 已实现拟合流程，待四折执行 | 有界参数拟合、训练折幅值估计、留一 MAT 切分 |
| `revision/evaluate.py` | 已实现评价流程，待留出结果确认 | ERP 指标、6 维收缩 LDA、两个冻结参数机制对照；首版必要检查已并入 |
| `revision/run_revision.py` | 已整理为唯一入口，待实际运行 | 串联审计、模拟、拟合、评价和输出 |

原 `revision/diagnostics.py` 草稿已移除；首版需要的检查并入 `evaluate.py`。原目录中的 `test_*.py` 仍是旧草稿检查，不代表完整模型已经验收。不要再创建一组重复的 `validation.py`、`classification.py`、`observation.py` 等小文件。

## TODO

### 0. 整理修正版入口与配置

- [ ] 检查当前 `revision/` 草稿文件和入口，列清可复用函数；保留能复用的内容，不把草稿通过状态当作验收结论。
- [ ] 完成 `config.py`：集中定义输入/输出路径、时间轴与事件、默认参数及界限、刺激/模板几何、G 和观测基函数。
- [ ] 固定随机种子、数组轴约定（时间始终为最后一维）、真实时间轴字段和输出目录。
- [ ] 确保旧 `01—05` 与两个历史诊断脚本没有被修改，旧输出目录没有被新版覆盖。

**验收：** 其他模块只从统一配置读取固定值；新版入口明确写入 `output/revision_v1/`。

### 1. 完成真实数据读取与条件审计

**文件：** `revision/real_data.py`（复用现有）、必要时整理 `revision/config.py`。

- [ ] 对 4 份 `*_clean.mat` 逐文件读取 `trial_data`、`relative_time`、`cue_type` 等字段，不跨 MAT 混合条件。
- [ ] 统一脑电通道为 `F3、Fz、F4`；保留通道顺序、时间轴、采样率和记录标识。
- [ ] Stage1 只使用 `cue_type=-1/+1` 对应的左右提示；其他值隔离、计数并记录原因。
- [ ] 生成固定数据清单：8 个 Stage1 左右主条件、2 个 A 记录双圆点控制条件、2 个 B 记录 `target_unknown` 汇总条件。
- [ ] 按预设 baseline 做逐试次校正；低于每条件 `n≥10` 的数据只描述，不进入拟合或分类。
- [ ] 不从不可靠的按键/目标标签推断 B 记录的 inward/outward 布局。

**验收：** 每个 MAT 单独有试次数、条件数、排除数和事件审计；未知标签不会被填成左右或 inward/outward。

### 2. 实现 LGN、V1 与三角构型前端

**文件：** 新建 `revision/frontend.py`；固定值使用 `config.py`。

- [ ] 按 256×256 标准刺激矩阵生成输入；按面积均值降到 64×64，保留原图坐标与运算网格的换算关系。
- [ ] 用有符号亮度变化构造刺激输入，保留白底、刺激 onset、Stage1 200 ms offset 和 Stage2 窗内持续显示的区别。
- [ ] 实现中心—周围 DoG、适应状态以及独立 ON/OFF 通路；对 LGN 输入保留原方案的 TCR、IN、TRN 群体动力学及固定参数。
- [ ] 将 LGN 的 ON/OFF 输出送入 Gabor，不在方向轴上预先平均；保存 4 个方向与空间位置。
- [ ] 用左右镜像的规范刺激构造两个三角模板；提取边缘支持点和顶点支持点，检查轮廓交并比与左右镜像编码。
- [ ] 同时输出通用边缘图 `B`、左模板响应 `H_L` 和右模板响应 `H_R`；对双圆点只记录模板交叉响应，不修改模板迎合标签。
- [ ] 执行 64/128/256 尺度审计；按方案中的核能量二阶矩和频谱峰检查。若 64 网格未过预设标准，在 EEG 拟合前冻结改用 128 网格。

**验收：** 左右规范刺激经过同一前端；输出包含 ON/OFF、4方向空间响应、`B/H_L/H_R` 和实际时间轴；尺度和模板检查有明确通过/失败记录。

### 3. 完成低维皮层、源代理和观测模型

**文件：** 改造 `revision/model.py`。

- [ ] 将 `B、H_L、H_R` 分别投影到固定的左右半域基函数，形成 3 类输入 × 2 个空间模式。
- [ ] 实现六个 E/I Wilson–Cowan 群体；使用固定耦合、时间常数和层级延迟，`τS`、`gI` 使用配置中的待拟合参数。
- [ ] 记录未裁剪状态并检测数值越界；不通过逐步裁剪掩盖不稳定。
- [ ] 用固定突触响应核形成 E−I 源代理；将共同/对比形状量按方案定义排列成 6 维源代理。
- [ ] 使用冻结的 G 和固定基函数得到两个可观测模式及 `F3/Fz/F4` 模拟波形；保持相对单位，不标为 μV。
- [ ] 报告 G 的秩 2 约束和真实数据中的 `u2` 未解释成分；不做源反演或解剖定位。
- [ ] 对 Stage1 左右默认响应执行 `dt=1 ms` 与 `dt=0.5 ms` 数值一致性检查。

**验收：** 从前端到三电极可重复运行；左右差异可沿构型、群体、源代理和观测逐层追踪；数值失败被显式报告。

### 4. 实现逐折拟合与幅值估计

**文件：** 新建 `revision/fit.py`。

- [ ] 实现 4 折留一 MAT：每折用另外 3 份记录训练，只用其 Stage1 左右数据拟合。
- [ ] 在 tau_a=40–160 ms 的预设剖面网格上依次拟合 tau_s/g_i；每个剖面运行一个固定主起点，只在全局最优 tau_a 上用其他两个起点复核；保存损失、收敛状态和边界标记。
- [ ] 按方案采用默认值加两个固定初值、每个初值最多 60 次目标评估；保存损失、收敛状态和边界标记。
- [ ] 每折仅用训练记录估计一个共享非负幅值系数 `a`；不得使用留出 ERP、峰时或分类结果选择参数。
- [ ] 训练记录左右等权、记录等权；候选无效、拟合失败、无内部正峰或晚峰截断都保留状态，不丢折。
- [ ] 让拟合函数输出可冻结的 `FitResult`，留出评估端不能写回或修改训练结果。

**验收：** 四折训练/留出边界清楚；改变留出数据或标签不会影响训练参数；没有超范围参数搜索。

### 5. 实现 ERP、分类与机制对照

**文件：** 新建 `revision/evaluate.py`；从现有 `revision/diagnostics.py` 迁入仍需功能。

- [ ] 对每折 Stage1 留出 ERP 计算相关、RMSE/NRMSE、左右效应、共同/侧化模式、峰值状态及 `u2` 能量；不插值伪造时间精度。
- [ ] 提取真实 Stage1 单试次 6 维特征：在 `[80,200)`、`[250,450)`、`[450,700] ms` 三个预设窗分别计算 `u0/u1` 均值；`u2` 不进入主分类。
- [ ] 训练一个收缩 LDA：标准化、类均值、共享协方差均只从训练试次估计；留出试次只产生分数和预测标签。
- [ ] 输出逐记录 BA、ROC-AUC、召回率、混淆矩阵及平局数；把分类结果解释为实测特征可分性，不作为机制正确的证明。
- [ ] 实现“去位置”对照：方向响应空间平均后广播并保持总量，使用同一构型、动力学、G 和幅值系数，不重新拟合。
- [ ] 实现“去 offset”对照：Stage1 刺激持续到 800 ms，不在 200 ms 恢复 baseline；其余保持不变，不称为真实 P300。
- [ ] 汇总双圆点冻结参数控制响应；`inward/outward` 只产生模型反事实，不与 `target_unknown` ERP 配对。
- [ ] 生成 3 张综合图：机制链、留出 ERP 比较、分类与机制对照。

**验收：** 真实 LDA 与前向模型 ERP 结论分开报告；训练/留出隔离可追溯；两个机制对照不重拟合参数。

### 6. 整理唯一运行入口与精简输出

**文件：** 改造 `revision/run_revision.py`。

- [ ] 按“数据审计 → 前端尺度审计 → 前向默认模型 → 四折拟合/评估 → 机制对照 → 汇总”的顺序调用模块。
- [ ] 支持独立的审计、模拟和完整验证运行模式；模式名称和参数在入口帮助信息中说明。
- [ ] 写入 `output/revision_v1/`，并保存输入、代码、固定配置的 hash 和每折运行状态。
- [ ] 只输出 6 类数据文件：`manifest.json`、`condition_audit.csv`、`fold_summary.csv`、`trial_scores.csv`、`mechanism_controls.csv`、`model_outputs.npz`；另保存 3 张综合图。
- [ ] `model_outputs.npz` 只存图表和复核所需的必要时间轴/曲线，不重复保存能由输入重算的全部中间数组。
- [ ] 确保部分失败时仍保留已完成折与失败原因；不得静默跳过或覆盖旧结果。

**验收：** 一条入口可复现完整流程；重复运行能核对输入/配置 hash；输出文件名和目录与方案一致。

### 7. 汇总实现状态与论文边界

**文件：** 新建 `documents/revision_v1_运行与验收报告.md`；必要时更新 `revision/README.md`。

- [ ] 按方案逐项记录正确性检查、尺度选择、拟合状态、留出 ERP、分类结果和机制对照。
- [ ] 报告失败折、被排除试次、无峰/截断峰、边界参数及不可评估指标。
- [ ] 明确结果是低维现象学前向模型、固定示意观测和跨 MAT 记录评价；不称跨被试验证、真实导联场或生理参数真值。
- [ ] 明确“完成代码实现”和“获得实测支持”是两种不同结论；效果不佳时如实降级，不追加无预注册参数搜索。
- [ ] 复核首版范围外的生成式判别、空间神经场、参数恢复和扩展消融没有混入主结果。

## 首版完成判据

- [ ] 7 个运行模块接口一致，旧版脚本与旧结果保持完整。
- [ ] 8 个 Stage1 条件进入主拟合/分类；圆点和未知布局严格按控制/描述用途处理。
- [ ] LGN→构型→低维 WC→源代理→三电极观测链可重复运行，所有数值异常均显式记录。
- [ ] 四折留一 MAT ERP 比较、6维收缩 LDA 和两个冻结参数机制对照全部完成；不以预设分类准确率作为代码完成门槛。
- [ ] 六类数据文件、三张综合图和运行验收报告齐全，结论与证据边界一致。

## 首版明确不做

- [ ] 不实现生成式似然比或共享模板残差校正。
- [ ] 不建立空间神经场，不增加 PFC、海马、LIF 或 Kuramoto 层。
- [ ] 不做参数恢复、完整可识别性分析、9维对照分类或大规模敏感性搜索。
- [ ] 不把 `target_unknown` 擅自标成 inward/outward，不把模拟反事实冒充真实条件。
- [ ] 不改写原 `01—05` 结果，不将模拟电位标成 μV，不将实现完成写成模型已验证。


## 测试后修复与复测（2026-09-24）

本轮按上一轮测试结果处理：旧探针不再作为当前接口验收；保留通过尺度审计选出的 128 网格；不以调参数追求更高分类分数。

- [x] **测试与接口**：将 `test_model.py`、`test_validation.py` 改为覆盖当前 `frontend/model/evaluate` 的行为测试；保留真实数据测试；完整 `pytest` 必须通过。
- [x] **左右标签审计**：核实 `cue_type=-1/+1` 的左右定义来自数据提取/绘图代码；对 4 份 MAT 分别报告左右试次数、特征差异方向和留一记录分类结果，不把 4 份记录称为 4 名被试。
- [x] **前端性能优化**：对三角构型的空间采样做向量化并预计算静态坐标；与现有 `revision_v1` 基线对齐比较响应数组，报告单条件前端耗时变化；不得改变滤波尺度、核参数或对条件单独归一化。
- [x] **模型论述边界**：沿用获批的低维功能性 Wilson–Cowan 皮层群体模型；README、manifest 及报告不得称其为显式 `V1→IT→PFC` 级联或解剖源定位。若该层级是后续硬性要求，再另行立项，不在本轮加入新自由度。
- [x] **完整复测**：重跑 `pytest`、尺度审计、数值步长检查、真实数据分类与四折留出 ERP 拟合；保留失败折和原始结果，不通过留出数据选参。
- [x] **结果与优化建议**：记录复测指标、运行时间、分类可分性及主要限制，据此确定后续优化，不设人为准确率门槛。

**本轮设计裁定：** 保留 `问题二_项目实施方案.md` 第 2.7 节的低维功能群体和固定秩二观测；当前结果不支持添加 V1/IT/PFC 层级或调大导联差异。代价是论文只能解释低维功能群体到三电极代理观测的计算假设，不能声称已实现皮层层级或精确解剖机制。

## revision_v3 架构修正与复测（2026-09-25）

本阶段是在独立的 `revision_v3/` 中落实统一驱动尺度、群体前馈、几何近似源到电极映射、第一问观测算子和拟合预算保护。原 `revision_v1/`、`revision_v2/` 及其输出保持不变。

- [x] 以规范左右刺激估计共享输入 RMS 尺度，不使用留出 EEG 标定前端尺度。
- [x] 给早期与形状/方向群体添加固定前馈延迟；将六个 E-I 源代理经固定几何示意导联映射到 F3/Fz/F4。
- [x] 模拟曲线在比较前经过第一问相同的滤波、降采样与逐通道基线校正算子。
- [x] 修复优化预算耗尽的返回状态；7 项 v3 合同测试通过，Python 编译检查通过。
- [x] 完成 128 网格、4 ms 前端步长审计和四折留一 MAT 拟合，生成 ERP 图、左右差异、固定 6 维 LDA 和两个冻结参数对照。
- [ ] **真实数据验证尚未通过：** 留出 ERP 平均 NRMSE 1.802，左右差异波形相关均值 0.173，6 维 LDA BA/AUC 为 0.453/0.421。
- [ ] **结构限制待解决或在论文中限定：** 模型左右差异能量几乎全在侧化模态，实测侧化能量均值约 16.8%；四折 `tau_s` 全到 100 ms 上界且候选未收敛。
- [ ] **完整事件上下文待补：** 模拟需要纳入 +2.2 s 目标事件后再执行整段零相位滤波；未知布局不可从反应标签直接伪造。

实施详情和数值证据见[revision_v3 实施结果与问题诊断](./revision_v3_实施结果与问题诊断.md)。

## 运行时间诊断与修复（2026-09-24）

- [x] 核实长时间运行时进程仍占用 CPU，定位为 tau_a 数值微分重复触发前端计算，不是死锁。
- [x] 修正 L-BFGS-B 有限差分步长，使参数试探可以改变 float32 模型状态。
- [x] 固定 tau_a 剖面候选点；每点前端缓存一次，并在默认/拟合模型生成时复用缓存。
- [x] 将三起点从每个 tau_a 候选重复运行，改为每个剖面一个主起点，对最优剖面保留另两个起点复核。
- [x] 在运行入口增加尺度审计、标准前向、dt 收敛、各折及 tau_a 剖面的进度日志。
- [x] 新增拟合调用结构回归测试；revision 套件共 22 项全部通过。
- [x] 单折实测用时从 136.0 s 降为 98.7 s；最优参数与损失与原密集多起点结果一致。
- [x] 完成修复后的四折全量 validate，检查输出状态、ERP 误差、实测分类、两项机制对照及旧版哈希。

- [x] 对机制对照的确定性前端增加跨折缓存后独立复算；8 行结果与原 CSV 关键数值完全一致，最大差为 0。

## 根因定位后的口径修正（2026-09-25）

**目标：** 修正审计遗漏，并让秩二观测模型只在其可表达的两个观测模式上拟合/验收；第三模式作为明确的未解释残差报告。保留既有参数与旧结果，不通过翻转真实 EEG、挪动事件或调大导联权重改善表观效果。

- [x] **事件通道审计：** `real_data.py` 单独读取 `VisCue`，逐 MAT 输出 cue 起始、持续时间及其与 `cue_type` 的一致率；`Action/TgtAct` 另列为动作/目标动作诊断，不将其误作 cue marker。Stage1 事件仍按 VisCue 对齐；Stage2 的 2.20 s 标为实验时序假设。
- [x] **秩二拟合口径：** `fit.py` 将实测与预测均投影到 `u0/u1` 两个可观测模式后计算主拟合损失；明确 `u2` 不参与参数估计。由于当前 G 的秩二结构使 `u2` 残差与模型参数无关，验证拟合参数/幅值不因移除该常数残差而改变。
- [x] **验收指标：** `evaluate.py` 同时报告 `u0/u1` 主验收误差/相关、全三电极次级误差，以及实测 `u2` RMS/能量占比；预测的 `u2` 固定为零，残差不得写成已解释的神经响应。
- [x] **测试先行：** 增加最小测试，覆盖 VisCue onset/offset 与标签一致率、动作通道分离，以及一个含非零 `u2` 的合成 ERP 只进入未解释残差而不改变秩二拟合结果；先观察测试按预期失败，再实现。
- [x] **快速实测核对：** 用四份真实 MAT 重建事件审计和 ERP 指标，核对 channel map、提示时序及 `u2` 残差；不因本次代数等价的目标函数改写重复运行完整参数网格拟合。
- [x] **报告与产物：** 更新 `condition_audit.csv`、manifest 与运行报告，写明 Stage1 对齐证据、Stage2 时间假设、秩二映射限制及真实数据分类仍接近机会水平；旧版运行产物保留作为基线。

**验收：** Stage1 cue 标记与 `cue_type` 一致；主拟合目标不包含不可表达的 `u2`；留出报告能区分模型可观测子空间误差与未解释 `u2`；测试通过；不把本次口径修正描述成已提升左右分类性能。
### 本轮执行结果（2026-09-25）

- 四份 MAT 的 VisCue 均从 0 ms 开始；最后一个激活采样为 195.3125 ms，首个不激活采样为 203.125 ms，覆盖名义 200 ms offset 的 128 Hz 采样边界。有效 VisCue 标记数分别为 54、80、83、80，标记方向与 `cue_type` 一致率均为 100%。

## revision_v3 第一轮完整场景与映射留出验证（2026-09-25）

- [x] 在独立 `revision_v3/` 中扩展 LGN 输入接口，支持完整场景序列与预热状态；旧静态刺激接口及原 v3 结果保持不变。
- [x] 构造 Stage1 场景：圆圈背景预热后持续至提示出现，0–200 ms 呈现圆圈+三角，offset 后恢复圆圈；在 2.2 s 加入 Task1 双圆点目标。Task2 未知布局以 inward/outward 两条前向响应等权平均作滤波上下文，不生成逐试次真标签。
- [x] 使用四份 MAT 原试次顺序分成前后时间块，双向训练/留出；所有 ERP 使用逐试次 `[-200,0) ms` 基线，并把模型源经过与 Q1 相同的完整 epoch 滤波、降采样与基线算子。
- [x] 在同一模型源上比较旧固定 G（仅训练块估共享增益）、带向固定 G 收缩的有效映射（α=0.1/1/10），并加训练块 ERP 持续预测基线；留出块未用于映射或 α 拟合。
- [x] 报告全通道、共同模式和左右条件差异指标，另计算两块分别估计的有效映射余弦稳定性。
- [x] 新增时间轴/岭映射/源语义行为测试；`compileall` 与 `pytest src/C/q2/revision_v3` 通过，11 项通过。完整前端和比较运行约 19 秒。
- [x] 输出留出指标表、逐记录汇总、映射系数/稳定性、左右差异图和论文口径结论，见 `output/revision_v3_round1/第一轮结果与结论.md`。
- [x] **结果判断：** α=1 有效 G 的全通道平均 NRMSE 为 0.998，旧固定 G 为 1.031；左右差异 NRMSE 为 1.012 vs 1.183。训练块 ERP 持续基线的全通道 NRMSE 为 0.811，整体更好。α=1 映射在两个 B 记录的双向稳定余弦仅 -0.425、0.151。因此固定 G 是部分记录的限制，但更灵活映射不是通用修复，现阶段神经模型尚未在留出 EEG 上胜过简单 ERP 基线。
- [x] **边界：** 圆圈开始时间未被 VisCue 标记；1200 ms LGN 预热加记录 epoch `-1000..0 ms` 等价于模型假定 cue 前至少约 2.2 s 圆圈状态稳定。这只是稳态初始化假设，未做预热时长敏感性；2.2 s 目标沿用 Q1 时序假设。

**下一步只建议做一项：** 在固定刺激输入和映射方案下，检查共同 ERP 在训练时间块的预测残差与参数边界；若共同响应仍不能跨块预测，再只调整少量共享动力学时间尺度，并始终与 ERP 持续预测基线比较。暂不继续扩张导联矩阵自由度，也不根据留出表现挑选 α。
- 四份 MAT 的原始脑电通道顺序均为 `Fz,F3,F4`；按 `DataLabel` 取索引 `1,0,2` 后输出 `F3,Fz,F4`。没有发现通道重排错误。`Action/TgtAct` 单独保留为动作通道诊断，没有拿它对齐 cue。
- 由于 G 为秩二，模型预测的 `u2=(F3-2Fz+F4)/sqrt(6)` 恒为零。实测条件平均 ERP 中 `u2` 能量占比为 0.21%–49.22%（中位数 7.10%）；该差异现作为未解释残差报告。
- 秩二可观测子空间上的留出拟合仍弱：8 个条件的 NRMSE 中位数 1.067（范围 0.933–3.045），`u0/u1` 平均相关中位数 -0.108（范围 -0.635–0.239）。左右条件差值的模拟幅度仅保留实测可观测差值的 5.2%–27.5%。因此这次口径修正没有提升拟合或分类结果。
- 真实数据六维 LDA 的跨记录均衡准确率为 0.453、AUC 为 0.421，接近机会水平。按固定种子 20260925 做 500 次左右条件 split-half，只有 `VisualCogB_Task-2` 的可观测差值较稳定；其余三份记录的中位相关分别为 0.032、-0.536、-0.436，提示真实左右差异跨记录/分半一致性不足。
- 结论：cue 事件对齐、cue 标签符号和通道顺序不是当前拟合失败的主要原因。已确认的结构性限制是秩二 G 无法表达实测 u2；但移除 u2 后可观测波形仍拟合不佳，不能把全部失败归因于导联矩阵。原四折优化未重跑：对任一候选参数而言，预测 u2 恒为零，剔除该项只减去常数并改变损失尺度，不改变最优参数。
- `pytest` 全套 25 项通过；`condition_audit.csv`、`fold_summary.csv` 与 manifest 已刷新，模型预测数组及旧版结果未重算或覆盖。

## 全时段左右信息解码诊断（2026-09-25）

**目的：** 检查固定时间窗分类是否漏掉提示 onset/offset 附近或更晚时段的真实左右信息。本分析是独立诊断，不改写 01–04 模型、拟合参数或既有输出。

- [x] 新建 `11_full_epoch_temporal_decoding.py`，逐 MAT 读取 `8riemann_denoise` 的清洗 EEG 与 `cue_type`，按 `F3/Fz/F4`、逐试次 `[-200,0) ms` 基线校正后分析提示后 `[0,2200) ms`。
- [x] 使用 100 ms 局部窗口的三通道均值和斜率作为固定特征；采用五段连续试次块留出交叉验证，缩放仅在训练折拟合。
- [x] 在每个连续验证块内置换左右标签、以四份记录和所有时间窗的最大 AUC 偏离作全局多重比较校正。
- [x] 保存逐时间窗解码、记录汇总、全时段图和分析清单；将 onset 250–500 ms 与约 200 ms offset 后 250–500 ms 作为参照标记，不把它们当作唯一分析窗。
- [x] 根据校正结果判断：若存在跨时间/记录校正后可重复的窗外可分性，再讨论模型时间特征；若没有，则记录为当前通道/样本下未检出稳定可解码信息，不据此盲调模型。

**执行结果：** 四份记录分别保留 54、80、83、80 个试次；每份记录按时间顺序分成 5 个连续验证块，每块均包含左右标签。每个 100 ms 窗提取 F3/Fz/F4 的均值与线性斜率，使用训练折内标准化的收缩 LDA；199 次置换在每个验证块内打乱标签，并用四份记录 × 22 个时间窗的最大 AUC 偏离作全局校正。

- 全局置换零分布的 95% 最大阈值为可分性 AUC 0.815；**全局校正后显著时间窗为 0**。
- 每份记录的最高表观可分性为 0.734（VisualCogA_Task-1，0–100 ms，校正后 p=0.645）、0.688（VisualCogA_Task-2，1100–1200 ms，p=0.970）、0.694（VisualCogB_Task-1，2100–2200 ms，p=0.950）及 0.627（VisualCogB_Task-2，900–1000 ms，p=1.000）。最佳窗分散在不同时间，没有跨记录一致的窗外峰。
- 每份记录的最高原始分数都出现在 onset 参考窗 250–500 ms 之外，但均未通过全局置换校正；不能将扫描得到的最大值解释成真实的窗外神经差异。
- 因而本轮**没有检出**在现有三电极、每 100 ms 均值/斜率表征下稳定的左右解码信息。它不能证明所有波形信息均不存在：不足 100 ms 的瞬态、个体内跨试次不稳定信息、未测电极或更高维表征仍未检验。199 次置换的 p 值分辨率为 0.005；当前最佳校正 p 均远离 0.05，无需为追逐阈值追加置换。
- 产物位于 `output/11_full_epoch_temporal_decoding/`：`逐窗左右解码.csv`、`记录汇总.csv`、`全时段左右解码.png`、`analysis_manifest.json`。本轮仅新增诊断脚本与产物，没有修改 01–04 模型参数、真实 EEG 源文件或既有结果。

## revision_v2 架构分支实现与留出评价（2026-09-25）

**目的：** 按新架构建议创建独立 `revision_v2`，保留 `revision_v1` 作为基线；实现左右模板对向编码和固定三模式观测，并在实际数据上检查效果。v2 不覆盖 v1，也不对留出记录选参。

- [x] 建立独立代码目录 `revision_v2/`，实现共同形状与左右对向的 Gabor 构型读出；左右镜像刺激交换左右模板响应，不进行镜像不变池化。
- [x] 保留低维 Wilson–Cowan 功能群体与既定参数规模；使用固定正交 `u0/u1/u2` 传感器坐标，不增加自由导联矩阵或解剖源解释。
- [x] 添加空间核尺度审计与前端时间步长审计；选用 128×128 空间网格、4 ms 特征步长和 1 ms 动力学积分。4 ms 与 1 ms 参考的核心特征相对 RMSE 约 2.4%，模型观测曲线相对误差约 0.23%。
- [x] 完成四折留一 MAT 拟合、旧 v1 冻结预测对照、固定参数路线/秩消融、真实 EEG 6/9 维留一记录分类、记录内置换和分半稳定性评价。
- [x] 修正控制台摘要，同时打印记录内 9 维置换检验的原始 p 与四记录 Holm 校正 p，避免只展示未经校正结果。
- [x] 验证：revision_v2 Python 文件编译通过，`pytest -q src/C/q2/revision_v2` 共 29 项通过；完整运行命令见 `revision_v2/README.md`。
- [x] 写入独立说明 `revision_v2/README.md`，记录架构、运行方式、产物、结果及解释边界。

**验收结果：** 真实 EEG 留一 MAT 的 6 维特征平衡准确率为 0.453（AUC 0.421），9 维为 0.479（AUC 0.448）；四记录 9 维置换检验经 Holm 校正后均不显著。v2 opponent rank-3 留出 ERP NRMSE 中位数为 0.9998、均值为 1.0341，零预测基线为 1.0000；左右差值幅值保留比例中位数约 0.065。rank-2/rank-3 结果几乎相同。

**结论：** 左右模板前端能够表达左右构型，但真实三电极 EEG 的左右特征跨记录不稳定；v2 正向模型未表现出优于零预测的绝对 ERP 拟合。本轮证明实现可运行，不证明模型已解释真实脑电。不能把分类或前端模板检查单独当作机制验证，也不应继续按留出结果调参。详细表格与图片位于 `output/revision_v2/`；实现说明见 [revision_v2 README](../revision_v2/README.md)。

## 最后一轮：左右提示 ERP 时间分块重复性（2026-09-25）

- [x] 基于 Q1 最终清洗 MAT，统一逐试次 `[-200,0) ms` 基线；不重复滤波、不改 v3/旧版模型参数。
- [x] 按原始试次顺序分前后块，分别计算左右共同 ERP 与右减左条件差；覆盖 onset `0–200 ms`、offset 后 `200–800 ms`、后续 `800–2200 ms` 和完整提示间隔。
- [x] 对 `0–800 ms` 主窗按块、按提示方向做 1200 次试次 bootstrap，并检查左右标签在试次序列中的分布。
- [x] 结果：共同 ERP 的完整时间窗跨块相关为 `0.633–0.878`；左右差值在 `200–800 ms` 仅两份记录呈较高点估计相关（A_Task-1 `0.911`、B_Task-2 `0.900`），另两份为 `-0.676` 与 `-0.207`。四份记录左右差值相关的 bootstrap 95% 区间均跨过 0，说明每半块单侧试次数不足以支持稳定效应结论。
- [x] 左右标签与试次序号的整体相关绝对值不超过 `0.124`，没有明显单调趋势；但连续小块比例有波动，不能排除局部不平衡与时间漂移共同作用。
- [x] 结论：目前不是“完全没有左右信息”，而是观察到的左右差异具有记录/时间块依赖性，尚无足够证据表明它在四份数据中稳定重复。共同 ERP 比左右差值更可重复；结合上一轮结果，现有模型既未稳定解释方向差异，也没有胜过直接 ERP 持续基线。不得据此归咎灰度刺激、单一导联矩阵或某个生理时间常数。
- [x] 输出位于 `output/12_split_half_erp_reliability/`：分半波形图、时间窗指标、bootstrap 区间、试次顺序/条件数表及解释报告。源数据、v3 参数和既有结果均未修改。
