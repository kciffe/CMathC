# 问题二代码实现 TODO\n\n> **For agentic workers:** 按任务顺序逐项实施；执行代码计划时使用 `superpowers:executing-plans`。每项通过对应验收条件后再勾选。\n\n**目标：** 在保留原始 `01—05` 脚本和结果的前提下，完成一个可复现的 LGN→视觉构型→低维 Wilson–Cowan→三电极观测正向模型，并用同一批真实记录完成留出 ERP 比较和左右响应分类。\n\n**架构：** 在 `q2/revision/` 整理为 7 个运行模块：`config.py`、`real_data.py`、`frontend.py`、`model.py`、`fit.py`、`evaluate.py`、`run_revision.py`。旧版诊断代码中首版需要的检查合并进 `evaluate.py`；不再按每个小步骤新增脚本。\n\n**技术环境：** 使用项目现有 Python 环境及已安装依赖；实现前确认 MAT 读取、数值计算和绘图所需库，不为可选扩展添加新依赖。\n\n**依据：** [《问题二项目实施方案》](./问题二_项目实施方案.md)\n\n**当前状态（2026-09-24）：** 七个运行模块已完成首轮方案化实现并落入 `revision/`；尚未执行运行、尺度审计或验收，因此下列检查框保持未勾选。实施状态见 [revision README](../revision/README.md)。\n\n## 全局约束\n\n- 原 `01_gabor_frontend.py`、`02_lgn.py`、`03_cortex.py`、`04_simulated_eeg.py`、`05_simulated_real_eeg_compare.py` 及历史诊断脚本保留，不覆盖、不改写结果。\n- 新版代码放在 `src/C/q2/revision/`，正式输出放在 `src/C/q2/output/revision_v1/`；不与 `output/revision/` 草稿混用。\n- 主拟合只用 4 份 MAT 的 8 个 Stage1 左右条件。两个 A 记录 Stage2 双圆点只作冻结参数控制；两个 B 记录 Stage2 只作 `target_unknown` 总体 ERP 描述。\n- `inward/outward` 仅为标准刺激矩阵生成的模型反事实，不与未知布局的真实 ERP 配对。\n- 首版不做生成式似然、空间神经场、参数恢复、完整可识别性、大规模参数扫描或为通过准确率门槛而调参。\n\n## 实施审查重点\n\n- **未知标签与条件分组：** `real_data.py`必须逐MAT保留记录边界；B记录Stage2保持`target_unknown`，不得按答案或猜测补标签。\n- **离散空间尺度：** `frontend.py`从同一256×256刺激分别生成64/128网格结果；尺度审计不通过时，先冻结采用128网格，再进行EEG拟合。\n- **onset/offset极性：** Stage1的0 ms出现与200 ms消失分别输入；Stage2目标在模拟窗内持续显示，不制造offset。\n- **训练/留出隔离：** `fit.py`和`evaluate.py`每折独立训练；标准化、参数、幅值系数和LDA均不得读取留出记录。\n- **观测秩限制：** `model.py`固定G秩为2；`evaluate.py`报告`u2`未解释成分，不把模拟与实测的拓扑缺口藏起来。\n\n## 文件清单\n\n| 文件 | 当前状态 | TODO 职责 |\n|---|---|---|\n| `revision/config.py` | 已按方案整理，待运行确认 | 汇总固定路径、时间、参数、模板几何及冻结观测矩阵，避免重复常量 |\n| `revision/real_data.py` | 已按方案调整，待数据审计确认 | 读取 MAT、整理条件、执行事件与质量审计、形成 ERP/试次数组 |\n| `revision/frontend.py` | 已实现主链代码，尺度门待执行 | 固定视觉刺激、LGN ON/OFF、Gabor 与三角构型编码 |\n| `revision/model.py` | 已重写主链，待数值运行 | 六个 E/I 群体、突触响应源代理、固定示意观测 |\n| `revision/fit.py` | 已实现拟合流程，待四折执行 | 有界参数拟合、训练折幅值估计、留一 MAT 切分 |\n| `revision/evaluate.py` | 已实现评价流程，待留出结果确认 | ERP 指标、6 维收缩 LDA、两个冻结参数机制对照；首版必要检查已并入 |\n| `revision/run_revision.py` | 已整理为唯一入口，待实际运行 | 串联审计、模拟、拟合、评价和输出 |\n\n原 `revision/diagnostics.py` 草稿已移除；首版需要的检查并入 `evaluate.py`。原目录中的 `test_*.py` 仍是旧草稿检查，不代表完整模型已经验收。不要再创建一组重复的 `validation.py`、`classification.py`、`observation.py` 等小文件。\n\n## TODO\n\n### 0. 整理修正版入口与配置\n\n- [ ] 检查当前 `revision/` 草稿文件和入口，列清可复用函数；保留能复用的内容，不把草稿通过状态当作验收结论。\n- [ ] 完成 `config.py`：集中定义输入/输出路径、时间轴与事件、默认参数及界限、刺激/模板几何、G 和观测基函数。\n- [ ] 固定随机种子、数组轴约定（时间始终为最后一维）、真实时间轴字段和输出目录。\n- [ ] 确保旧 `01—05` 与两个历史诊断脚本没有被修改，旧输出目录没有被新版覆盖。\n\n**验收：** 其他模块只从统一配置读取固定值；新版入口明确写入 `output/revision_v1/`。\n\n### 1. 完成真实数据读取与条件审计\n\n**文件：** `revision/real_data.py`（复用现有）、必要时整理 `revision/config.py`。\n\n- [ ] 对 4 份 `*_clean.mat` 逐文件读取 `trial_data`、`relative_time`、`cue_type` 等字段，不跨 MAT 混合条件。\n- [ ] 统一脑电通道为 `F3、Fz、F4`；保留通道顺序、时间轴、采样率和记录标识。\n- [ ] Stage1 只使用 `cue_type=-1/+1` 对应的左右提示；其他值隔离、计数并记录原因。\n- [ ] 生成固定数据清单：8 个 Stage1 左右主条件、2 个 A 记录双圆点控制条件、2 个 B 记录 `target_unknown` 汇总条件。\n- [ ] 按预设 baseline 做逐试次校正；低于每条件 `n≥10` 的数据只描述，不进入拟合或分类。\n- [ ] 不从不可靠的按键/目标标签推断 B 记录的 inward/outward 布局。\n\n**验收：** 每个 MAT 单独有试次数、条件数、排除数和事件审计；未知标签不会被填成左右或 inward/outward。\n\n### 2. 实现 LGN、V1 与三角构型前端\n\n**文件：** 新建 `revision/frontend.py`；固定值使用 `config.py`。\n\n- [ ] 按 256×256 标准刺激矩阵生成输入；按面积均值降到 64×64，保留原图坐标与运算网格的换算关系。\n- [ ] 用有符号亮度变化构造刺激输入，保留白底、刺激 onset、Stage1 200 ms offset 和 Stage2 窗内持续显示的区别。\n- [ ] 实现中心—周围 DoG、适应状态以及独立 ON/OFF 通路；对 LGN 输入保留原方案的 TCR、IN、TRN 群体动力学及固定参数。\n- [ ] 将 LGN 的 ON/OFF 输出送入 Gabor，不在方向轴上预先平均；保存 4 个方向与空间位置。\n- [ ] 用左右镜像的规范刺激构造两个三角模板；提取边缘支持点和顶点支持点，检查轮廓交并比与左右镜像编码。\n- [ ] 同时输出通用边缘图 `B`、左模板响应 `H_L` 和右模板响应 `H_R`；对双圆点只记录模板交叉响应，不修改模板迎合标签。\n- [ ] 执行 64/128/256 尺度审计；按方案中的核能量二阶矩和频谱峰检查。若 64 网格未过预设标准，在 EEG 拟合前冻结改用 128 网格。\n\n**验收：** 左右规范刺激经过同一前端；输出包含 ON/OFF、4方向空间响应、`B/H_L/H_R` 和实际时间轴；尺度和模板检查有明确通过/失败记录。\n\n### 3. 完成低维皮层、源代理和观测模型\n\n**文件：** 改造 `revision/model.py`。\n\n- [ ] 将 `B、H_L、H_R` 分别投影到固定的左右半域基函数，形成 3 类输入 × 2 个空间模式。\n- [ ] 实现六个 E/I Wilson–Cowan 群体；使用固定耦合、时间常数和层级延迟，`τS`、`gI` 使用配置中的待拟合参数。\n- [ ] 记录未裁剪状态并检测数值越界；不通过逐步裁剪掩盖不稳定。\n- [ ] 用固定突触响应核形成 E−I 源代理；将共同/对比形状量按方案定义排列成 6 维源代理。\n- [ ] 使用冻结的 G 和固定基函数得到两个可观测模式及 `F3/Fz/F4` 模拟波形；保持相对单位，不标为 μV。\n- [ ] 报告 G 的秩 2 约束和真实数据中的 `u2` 未解释成分；不做源反演或解剖定位。\n- [ ] 对 Stage1 左右默认响应执行 `dt=1 ms` 与 `dt=0.5 ms` 数值一致性检查。\n\n**验收：** 从前端到三电极可重复运行；左右差异可沿构型、群体、源代理和观测逐层追踪；数值失败被显式报告。\n\n### 4. 实现逐折拟合与幅值估计\n\n**文件：** 新建 `revision/fit.py`。\n\n- [ ] 实现 4 折留一 MAT：每折用另外 3 份记录训练，只用其 Stage1 左右数据拟合。\n- [ ] 在 tau_a=40–160 ms 的预设剖面网格上依次拟合 tau_s/g_i；每个剖面运行一个固定主起点，只在全局最优 tau_a 上用其他两个起点复核；保存损失、收敛状态和边界标记。\n- [ ] 按方案采用默认值加两个固定初值、每个初值最多 60 次目标评估；保存损失、收敛状态和边界标记。\n- [ ] 每折仅用训练记录估计一个共享非负幅值系数 `a`；不得使用留出 ERP、峰时或分类结果选择参数。\n- [ ] 训练记录左右等权、记录等权；候选无效、拟合失败、无内部正峰或晚峰截断都保留状态，不丢折。\n- [ ] 让拟合函数输出可冻结的 `FitResult`，留出评估端不能写回或修改训练结果。\n\n**验收：** 四折训练/留出边界清楚；改变留出数据或标签不会影响训练参数；没有超范围参数搜索。\n\n### 5. 实现 ERP、分类与机制对照\n\n**文件：** 新建 `revision/evaluate.py`；从现有 `revision/diagnostics.py` 迁入仍需功能。\n\n- [ ] 对每折 Stage1 留出 ERP 计算相关、RMSE/NRMSE、左右效应、共同/侧化模式、峰值状态及 `u2` 能量；不插值伪造时间精度。\n- [ ] 提取真实 Stage1 单试次 6 维特征：在 `[80,200)`、`[250,450)`、`[450,700] ms` 三个预设窗分别计算 `u0/u1` 均值；`u2` 不进入主分类。\n- [ ] 训练一个收缩 LDA：标准化、类均值、共享协方差均只从训练试次估计；留出试次只产生分数和预测标签。\n- [ ] 输出逐记录 BA、ROC-AUC、召回率、混淆矩阵及平局数；把分类结果解释为实测特征可分性，不作为机制正确的证明。\n- [ ] 实现“去位置”对照：方向响应空间平均后广播并保持总量，使用同一构型、动力学、G 和幅值系数，不重新拟合。\n- [ ] 实现“去 offset”对照：Stage1 刺激持续到 800 ms，不在 200 ms 恢复 baseline；其余保持不变，不称为真实 P300。\n- [ ] 汇总双圆点冻结参数控制响应；`inward/outward` 只产生模型反事实，不与 `target_unknown` ERP 配对。\n- [ ] 生成 3 张综合图：机制链、留出 ERP 比较、分类与机制对照。\n\n**验收：** 真实 LDA 与前向模型 ERP 结论分开报告；训练/留出隔离可追溯；两个机制对照不重拟合参数。\n\n### 6. 整理唯一运行入口与精简输出\n\n**文件：** 改造 `revision/run_revision.py`。\n\n- [ ] 按“数据审计 → 前端尺度审计 → 前向默认模型 → 四折拟合/评估 → 机制对照 → 汇总”的顺序调用模块。\n- [ ] 支持独立的审计、模拟和完整验证运行模式；模式名称和参数在入口帮助信息中说明。\n- [ ] 写入 `output/revision_v1/`，并保存输入、代码、固定配置的 hash 和每折运行状态。\n- [ ] 只输出 6 类数据文件：`manifest.json`、`condition_audit.csv`、`fold_summary.csv`、`trial_scores.csv`、`mechanism_controls.csv`、`model_outputs.npz`；另保存 3 张综合图。\n- [ ] `model_outputs.npz` 只存图表和复核所需的必要时间轴/曲线，不重复保存能由输入重算的全部中间数组。\n- [ ] 确保部分失败时仍保留已完成折与失败原因；不得静默跳过或覆盖旧结果。\n\n**验收：** 一条入口可复现完整流程；重复运行能核对输入/配置 hash；输出文件名和目录与方案一致。\n\n### 7. 汇总实现状态与论文边界\n\n**文件：** 新建 `documents/revision_v1_运行与验收报告.md`；必要时更新 `revision/README.md`。\n\n- [ ] 按方案逐项记录正确性检查、尺度选择、拟合状态、留出 ERP、分类结果和机制对照。\n- [ ] 报告失败折、被排除试次、无峰/截断峰、边界参数及不可评估指标。\n- [ ] 明确结果是低维现象学前向模型、固定示意观测和跨 MAT 记录评价；不称跨被试验证、真实导联场或生理参数真值。\n- [ ] 明确“完成代码实现”和“获得实测支持”是两种不同结论；效果不佳时如实降级，不追加无预注册参数搜索。\n- [ ] 复核首版范围外的生成式判别、空间神经场、参数恢复和扩展消融没有混入主结果。\n\n## 首版完成判据\n\n- [ ] 7 个运行模块接口一致，旧版脚本与旧结果保持完整。\n- [ ] 8 个 Stage1 条件进入主拟合/分类；圆点和未知布局严格按控制/描述用途处理。\n- [ ] LGN→构型→低维 WC→源代理→三电极观测链可重复运行，所有数值异常均显式记录。\n- [ ] 四折留一 MAT ERP 比较、6维收缩 LDA 和两个冻结参数机制对照全部完成；不以预设分类准确率作为代码完成门槛。\n- [ ] 六类数据文件、三张综合图和运行验收报告齐全，结论与证据边界一致。\n\n## 首版明确不做\n\n- [ ] 不实现生成式似然比或共享模板残差校正。\n- [ ] 不建立空间神经场，不增加 PFC、海马、LIF 或 Kuramoto 层。\n- [ ] 不做参数恢复、完整可识别性分析、9维对照分类或大规模敏感性搜索。\n- [ ] 不把 `target_unknown` 擅自标成 inward/outward，不把模拟反事实冒充真实条件。\n- [ ] 不改写原 `01—05` 结果，不将模拟电位标成 μV，不将实现完成写成模型已验证。\n\n\n## 测试后修复与复测（2026-09-24）\n\n本轮按上一轮测试结果处理：旧探针不再作为当前接口验收；保留通过尺度审计选出的 128 网格；不以调参数追求更高分类分数。\n\n- [x] **测试与接口**：将 `test_model.py`、`test_validation.py` 改为覆盖当前 `frontend/model/evaluate` 的行为测试；保留真实数据测试；完整 `pytest` 必须通过。\n- [x] **左右标签审计**：核实 `cue_type=-1/+1` 的左右定义来自数据提取/绘图代码；对 4 份 MAT 分别报告左右试次数、特征差异方向和留一记录分类结果，不把 4 份记录称为 4 名被试。\n- [x] **前端性能优化**：对三角构型的空间采样做向量化并预计算静态坐标；与现有 `revision_v1` 基线对齐比较响应数组，报告单条件前端耗时变化；不得改变滤波尺度、核参数或对条件单独归一化。\n- [x] **模型论述边界**：沿用获批的低维功能性 Wilson–Cowan 皮层群体模型；README、manifest 及报告不得称其为显式 `V1→IT→PFC` 级联或解剖源定位。若该层级是后续硬性要求，再另行立项，不在本轮加入新自由度。\n- [x] **完整复测**：重跑 `pytest`、尺度审计、数值步长检查、真实数据分类与四折留出 ERP 拟合；保留失败折和原始结果，不通过留出数据选参。\n- [x] **结果与优化建议**：记录复测指标、运行时间、分类可分性及主要限制，据此确定后续优化，不设人为准确率门槛。\n\n**本轮设计裁定：** 保留 `问题二_项目实施方案.md` 第 2.7 节的低维功能群体和固定秩二观测；当前结果不支持添加 V1/IT/PFC 层级或调大导联差异。代价是论文只能解释低维功能群体到三电极代理观测的计算假设，不能声称已实现皮层层级或精确解剖机制。\n\n## 运行时间诊断与修复（2026-09-24）\n\n- [x] 核实长时间运行时进程仍占用 CPU，定位为 tau_a 数值微分重复触发前端计算，不是死锁。\n- [x] 修正 L-BFGS-B 有限差分步长，使参数试探可以改变 float32 模型状态。\n- [x] 固定 tau_a 剖面候选点；每点前端缓存一次，并在默认/拟合模型生成时复用缓存。\n- [x] 将三起点从每个 tau_a 候选重复运行，改为每个剖面一个主起点，对最优剖面保留另两个起点复核。\n- [x] 在运行入口增加尺度审计、标准前向、dt 收敛、各折及 tau_a 剖面的进度日志。\n- [x] 新增拟合调用结构回归测试；revision 套件共 22 项全部通过。\n- [x] 单折实测用时从 136.0 s 降为 98.7 s；最优参数与损失与原密集多起点结果一致。\n- [x] 完成修复后的四折全量 validate，检查输出状态、ERP 误差、实测分类、两项机制对照及旧版哈希。\n\n- [x] 对机制对照的确定性前端增加跨折缓存后独立复算；8 行结果与原 CSV 关键数值完全一致，最大差为 0。\n